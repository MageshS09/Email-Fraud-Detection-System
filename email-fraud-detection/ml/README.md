# ML Module - Email Fraud Detection

## Setup

```bash
cd ml
pip install -r requirements.txt
```

## Train Model

```bash
python train.py
```

The trained model is saved to `ml/models/email_fraud_model.joblib`.
